import { Typography } from "@mui/material";
import { myFont } from "./SectionOne";

export default function SectionTwelve() {
  return (
    <Typography variant="body1" fontWeight={500} className={myFont.className} color="#590112" fontSize={'65px'} textAlign={'center'}>
      Hope to see you at our wedding!
    </Typography>
  );
}